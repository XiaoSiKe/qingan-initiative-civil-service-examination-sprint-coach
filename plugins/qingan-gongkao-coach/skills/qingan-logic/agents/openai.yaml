interface:
  display_name: "青岸·逻辑判断教练"
  short_description: "训练定义、类比、条件推理与削弱加强"
  brand_color: "#2F6F5E"
  default_prompt: "使用 $qingan-logic 带我做一道判断推理题，先让我回答，再定位我的第一个推理断点。"
policy:
  allow_implicit_invocation: true
